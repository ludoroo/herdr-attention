{
  "name": "herdr-attention",
  "version": "0.1.0",
  "private": true,
  "description": "A focused Herdr view for agents that need attention",
  "license": "MIT",
  "repository": "github:ludoroo/herdr-attention",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "test": "node --test tests/*.test.mjs"
  }
}
